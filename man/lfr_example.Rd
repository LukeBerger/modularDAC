% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lfr_example.R
\docType{data}
\name{lfr_example}
\alias{lfr_example}
\title{Example expression dataset with known modular structure}
\format{
A \code{SummarizedExperiment} with 120 features and 60 samples:
\describe{
\item{assay "expression"}{a 120 x 60 numeric matrix of simulated expression values}
\item{rowData$gene}{feature name, \code{Gene_001} to \code{Gene_120}}
\item{rowData$true.module}{the simulated community, 1 to 3}
\item{colData$sample}{sample name, \code{Sample_01} to \code{Sample_60}}
\item{metadata}{the simulation call, the true module count and the true edge count}
}
}
\source{
Generated by \code{data-raw/make_example_data.R} on the dev branch,
using \code{make_lfr()} and \code{sim_graph_data()} with seed 1.
}
\usage{
lfr_example
}
\description{
A small simulated dataset used by the package README and examples. Expression
values were drawn from a covariance structure implied by a
Lancichinetti-Fortunato-Radicchi (LFR) benchmark graph of 120 nodes resolving
into three communities of 44, 40 and 36 nodes, joined by 378 edges in total.
}
\details{
It is a \code{SummarizedExperiment} rather than a bare matrix so it matches
the shape most users' data arrives in; modularDAC's own functions take the
assay matrix, which must be features (rows) by samples (columns).

\code{rowData} carries the community each feature was simulated from, so a
detected partition can be compared against the structure the data was built
from. Real data has no such column.

Loading this dataset requires the \pkg{SummarizedExperiment} package.
}
\examples{
if (requireNamespace("SummarizedExperiment", quietly = TRUE)) {
  data(lfr_example)
  x <- SummarizedExperiment::assay(lfr_example, "expression")
  dim(x)
  table(SummarizedExperiment::rowData(lfr_example)$true.module)
}
}
\keyword{datasets}
