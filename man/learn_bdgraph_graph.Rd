% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02.LearnGraphs.R
\name{learn_bdgraph_graph}
\alias{learn_bdgraph_graph}
\title{Learn a gene co-expression graph from a data matrix using BDgraph}
\usage{
learn_bdgraph_graph(
  x,
  method = c("ggm", "gcgm"),
  algorithm = c("bdmcmc", "rjmcmc"),
  iter = 5000,
  burnin = floor(iter/2),
  g.prior = 0.5,
  df.prior = 3,
  prob.cut = 0.5,
  pos.cut = 0,
  neg.cut = 0,
  ...
)
}
\arguments{
\item{x}{a numeric matrix with p features (rows) and n samples (columns)}

\item{method}{a character, the BDgraph inference method; 'ggm' for Gaussian graphical models (continuous data) or 'gcgm' for Gaussian copula graphical models (non-Gaussian / mixed data)}

\item{algorithm}{a character, the sampling algorithm passed to BDgraph::bdgraph; either 'bdmcmc' (birth-death MCMC) or 'rjmcmc' (reversible-jump MCMC)}

\item{iter}{an integer, the number of MCMC sampling iterations}

\item{burnin}{an integer, the number of burn-in iterations to discard (must be < iter); defaults to half of iter}

\item{g.prior}{a numeric between 0 and 1 (or a p x p matrix), the prior probability of edge inclusion}

\item{df.prior}{a numeric > 2, the degrees of freedom of the G-Wishart prior on the precision matrix}

\item{prob.cut}{a numeric between 0 and 1, the posterior edge-inclusion probability at or above which an edge is retained (0.5 selects the Bayesian median-probability model)}

\item{pos.cut}{a numeric, the minimum absolute partial correlation for a positive edge to be retained}

\item{neg.cut}{a numeric, the minimum absolute partial correlation for a negative edge to be retained}

\item{...}{additional arguments passed to BDgraph::bdgraph}
}
\value{
a named list with two elements: 'graph', the learned weighted igraph object whose edge weights are the partial correlations of the retained edges, and 'weights', the symmetric partial correlation matrix derived from the posterior precision matrix
}
\description{
Learn a gene co-expression graph from a data matrix using BDgraph
}
