% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02.LearnGraphs.R
\name{learn_GENIE3_graph}
\alias{learn_GENIE3_graph}
\title{Learn a gene co-expression graph from a data matrix using GENIE3}
\usage{
learn_GENIE3_graph(
  x,
  regulators = NULL,
  targets = NULL,
  tree.method = c("RF", "ET"),
  K = "sqrt",
  n.trees = 1000,
  symmetrize = c("max", "mean", "min"),
  percentile.threshold = 0.95,
  weight.threshold = NULL,
  n.cores = 1
)
}
\arguments{
\item{x}{a numeric matrix with p features (rows) and n samples (columns)}

\item{regulators}{a character or integer vector, the subset of features (by name or row index) treated as candidate regulators; if NULL all features are considered potential regulators}

\item{targets}{a character or integer vector, the subset of features (by name or row index) whose regulators are inferred; if NULL all features are treated as targets}

\item{tree.method}{a character, the tree-based ensemble method used to compute the variable importances; either 'RF' (Random Forests) or 'ET' (Extra-Trees)}

\item{K}{the number of candidate regulators randomly considered at each tree node; one of 'sqrt' (the square root of the number of regulators), 'all' (all regulators), or a positive integer}

\item{n.trees}{an integer, the number of trees grown in each feature's ensemble}

\item{symmetrize}{a character, how the directed GENIE3 importance matrix is collapsed into a symmetric one before building the undirected graph; one of 'max', 'mean', or 'min' of the two link directions}

\item{percentile.threshold}{a numeric, the top percentile of edges by importance weight will be kept in the final graph}

\item{weight.threshold}{a numeric, a fixed importance-weight threshold that overrides the percentile; edges below this strength will be removed}

\item{n.cores}{an integer, the number of cores GENIE3 uses to parallelise the computation}
}
\value{
a named list with two elements: 'graph', the learned weighted igraph object, and 'weights', the symmetric GENIE3 importance (regulatory-link) matrix
}
\description{
Learn a gene co-expression graph from a data matrix using GENIE3
}
