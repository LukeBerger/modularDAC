% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01.DetectModules.R
\name{find_WGCNA_mods}
\alias{find_WGCNA_mods}
\title{Detect co-expression modules from a data matrix using WGCNA}
\usage{
find_WGCNA_mods(
  x,
  min.size = 10,
  max.size = NULL,
  min.sft = 0.85,
  beta = NULL,
  cor.FN = c("bicor", "cor"),
  powers = c(seq(1, 10, by = 1), seq(12, 20, by = 2)),
  hclust.method = "average",
  cut.height = NULL,
  merge = FALSE,
  merging.cut = 0.2,
  iterate = TRUE,
  assign.by = c("adjacency", "eigengene"),
  deep.split = 4,
  min.natural.mods = 2L,
  retry.betas = TRUE
)
}
\arguments{
\item{x}{a numeric matrix with p features (rows) and n samples (columns)}

\item{min.size}{an integer, the minimum number of nodes allowed in a module (passed to cutreeDynamic)}

\item{max.size}{an integer, the maximum number of nodes allowed in a module}

\item{min.sft}{a numeric between 0 and 1, the minimum R-squared threshold for soft-thresholding power selection}

\item{beta}{an integer, the soft-thresholding power for WGCNA::adjacency; if NULL it is selected automatically via pickSoftThreshold}

\item{cor.FN}{a character, the correlation function to use in WGCNA::adjacency; either 'bicor' or 'cor'}

\item{powers}{an integer vector, candidate soft-thresholding powers evaluated by WGCNA::pickSoftThreshold}

\item{hclust.method}{a character, the agglomeration method passed to flashClust}

\item{cut.height}{a numeric between 0 and 1, the dendrogram cut height used by cutreeDynamic}

\item{merge}{a logical, if TRUE adjacent modules are merged using WGCNA::mergeCloseModules}

\item{merging.cut}{a numeric between 0 and 1, the eigengene dissimilarity threshold for WGCNA::mergeCloseModules}

\item{iterate}{a logical, if TRUE oversized modules are recursively split so every module fits within max.size}

\item{assign.by}{a character, how unassigned nodes are recruited to modules: 'adjacency' uses WGCNA adjacency, 'eigengene' uses correlation with module eigengenes}

\item{deep.split}{an integer 0-4 passed to \code{dynamicTreeCut::cutreeDynamic}, controlling how aggressively the dendrogram is split; 4 (the default, and the previously hard-coded value) splits most aggressively, 0 least}

\item{min.natural.mods}{an integer, the minimum number of modules the dynamic tree cut must produce before the partition is accepted; if fewer are found and \code{retry.betas} is TRUE the soft-threshold power is re-tried (see \code{retry.betas}). Fewer than 1 is always an error, since a cut that assigns every feature to the unassigned group cannot be repaired downstream}

\item{retry.betas}{a logical; if TRUE (default) and the soft-threshold power was chosen automatically, a natural cut yielding fewer than \code{min.natural.mods} modules is re-tried at the remaining \code{powers}, best scale-free fit first, keeping the first power that resolves the network into modules. Ignored when \code{beta} is supplied explicitly}
}
\value{
a list containing: 'wgcna.adj', the WGCNA co-expression adjacency matrix; 'initial.mods', the natural modules after merging and unassigned-node recruitment; and 'final.mods', those modules after recursive splitting to satisfy max.size
}
\description{
Detect co-expression modules from a data matrix using WGCNA
}
