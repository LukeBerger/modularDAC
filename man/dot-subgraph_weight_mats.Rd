% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03.Multithreaded_DaC.R
\name{.subgraph_weight_mats}
\alias{.subgraph_weight_mats}
\title{Collect a per-module weight matrix for each learned sub-graph}
\usage{
.subgraph_weight_mats(learned.graphs, other.outputs, x)
}
\arguments{
\item{learned.graphs}{a list of igraph objects, one per module (the parsed learned graphs)}

\item{other.outputs}{a list aligned with learned.graphs holding each parser's extra outputs; entries that are feature-named numeric matrices are used as that module's weight matrix}

\item{x}{a numeric matrix with p features (rows) and n samples (columns); its row names define the feature universe}
}
\value{
a list aligned with learned.graphs, each element a feature-named weight matrix or NULL when the module exposes no weights
}
\description{
For each module the weight matrix is taken, in order of preference, from the
feature-named numeric matrix supplied by the output parser (e.g. a partial
correlation matrix) and otherwise from the weighted sub-graph's adjacency
matrix. A module that exposes neither returns NULL, signalling that no combined
weight matrix can be built from it.
}
\keyword{internal}
