% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01.DetectModules.R
\name{.assign_unassigned}
\alias{.assign_unassigned}
\title{Helper to find_WGCNA_mods that assigns unassigned nodes (module 0) to an
existing module, either by WGCNA adjacency or by correlation with module eigengenes}
\usage{
.assign_unassigned(
  index.vector,
  adj = NULL,
  x = NULL,
  method = c("adjacency", "eigengene"),
  start.threshold = 0.95,
  step = 0.05,
  verbose = FALSE
)
}
\arguments{
\item{index.vector}{an integer vector of length p, assigning each node to a module (0 = unassigned)}

\item{adj}{a p x p numeric adjacency matrix produced by WGCNA::adjacency (diagonal expected to be 0); required when method = 'adjacency'}

\item{x}{a numeric matrix with p features (rows) and n samples (columns); required when method = 'eigengene'}

\item{method}{a character, how to score candidate modules for an unassigned node: 'adjacency' uses the maximum adjacency to an assigned node, 'eigengene' uses the maximum absolute correlation with a module eigengene}

\item{start.threshold}{a numeric, the initial score a node must exceed to be recruited to a module}

\item{step}{a numeric, the amount the threshold is lowered after a pass that recruits no nodes}

\item{verbose}{a logical, if TRUE the number of nodes recruited at each threshold is printed}
}
\value{
an integer vector of length p with unassigned nodes assigned where possible
}
\description{
Helper to find_WGCNA_mods that assigns unassigned nodes (module 0) to an
existing module, either by WGCNA adjacency or by correlation with module eigengenes
}
\keyword{internal}
